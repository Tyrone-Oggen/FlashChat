platform :ios, '14.0'

target 'Flash Chat' do
  use_frameworks!

  # Pods for Flash Chat
  pod 'CLTypingLabel'
  
  # Firebase related pods
  inhibit_all_warnings!
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
end
